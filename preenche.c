#include "preenche.h"
/*void preenche(int **distancia, int tam){
    int i,j;
    for (i = 0;i < tam;i++)
        for (j = 0;j < tam;j++){
            if(i!=j){
                distancia[i][j] = 1;
            }
            else distancia[i][j] = 0;
            printf("%d ", distancia[i][j]);

            if(j == tam - 1) printf(" \n");
        }
}*/
